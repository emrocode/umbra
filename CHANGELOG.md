New Release :tada:

- Respect `useStorage` before enabling theme sync between tabs
- Only auto-match system theme when `autoMatchTheme` is enabled
- Avoid redundant theme re-application when theme is unchanged
